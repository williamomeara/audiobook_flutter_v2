name: playback
description: "Playback controller with buffering and prefetch for audiobook TTS"
version: 0.0.1

environment:
  sdk: ^3.7.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  core_domain:
    path: ../core_domain
  tts_engines:
    path: ../tts_engines
  just_audio: ^0.10.5
  audio_session: ^0.1.21
  logging: ^1.3.0
  battery_plus: ^6.2.0  # Phase 2: Battery-aware prefetch
  shared_preferences: ^2.3.5  # Phase 4: Engine config persistence

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
