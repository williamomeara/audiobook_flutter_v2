name: playback
description: "Playback controller with buffering and prefetch for audiobook TTS"
version: 0.0.1

environment:
  sdk: ^3.10.4
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  core_domain:
    path: ../core_domain
  tts_engines:
    path: ../tts_engines
  just_audio: ^0.10.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
